# Maven Central credentials (will be overridden by environment variables)
mavenCentralUsername=
mavenCentralPassword=

# GPG signing
signing.keyId=
signing.password=
signing.key=

# Gradle settings
org.gradle.jvmargs=-Xmx2048m
org.gradle.parallel=true
org.gradle.caching=true
